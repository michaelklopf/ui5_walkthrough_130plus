<mvc:View
  controller="mk.tutorial.controller.Detail"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc">
  <Page
    title="{i18n>detailPageTitle}">
    <ObjectHeader
      intro="{invoice>ShipperName}"
      title="{invoice>ProductName}" />
    <content>
      <Label
        id="textInDetail"
        text="{invoice>ShipperName}" />
    </content>
  </Page>
</mvc:View>
